# Global settings
[general]
mode = viewer
# Use overlay if supported by your version; otherwise delete next line.
overlay = yes
position = auto
size = image
decoration = no
app_id = swayimg-preview
sigusr1 = reload
sigusr2 = next_file

# Split config into separate files (no equals sign!)
include keys-viewer.conf
include keys-gallery.conf
include keys-slideshow.conf

# Single-image viewer tuning
[viewer]
window = extend
transparency = grid
scale = optimal
position = center
antialiasing = mks13
loop = yes
history = 16
preload = 8

# Slideshow
[slideshow]
time = 5
window = mirror
transparency = #00000080
scale = fit
position = center
antialiasing = mks13

# Gallery (thumbnail grid)
[gallery]
size = 256
cache = 400
preload = yes
pstore = yes
fill = yes
antialiasing = mks13
window = #00000000
background = #1f2937ff
select = #334155ff
border = #000000ff
shadow = #000000ff

# Image list discovery/sorting
[list]
order = alpha
reverse = no
recursive = yes
all = no
fsmon = yes

# Text and font
[font]
name = JetBrainsMono Nerd Font
size = 14
color = #e5e7ebff
shadow = #000000d0
background = #00000000

# Info overlay defaults
[info]
show = yes
info_timeout = 0
status_timeout = 3

# Per-mode info layouts (titles with '+')
[info.viewer]
top_left = +name,+format,+filesize,+imagesize,+exif
top_right = +index,frame
bottom_left = +scale
bottom_right = status

[info.gallery]
top_left = +name
top_right = +imagesize
bottom_left = +index
bottom_right = status

[info.slideshow]
top_left = +name,+format
top_right = +index,frame
bottom_left = +scale
bottom_right = status
