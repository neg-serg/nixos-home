#!/usr/bin/env zsh
swayimg -g -f -s optimal -F <(
  find "$@" -type f -printf '%C@ %p\n' \
  | sort -rn \
  | cut -d ' ' -f 2- \
  | cut -d ':' -f 1 \
  | ug -G -iE '\.(jpe?g|png|gif|svg|webp|tiff|heif|avif|ico|bmp)$'
)
