diff --git a/src/main.rs b/src/main.rs
index cf2f7e7..c53e08f 100644
--- a/src/main.rs
+++ b/src/main.rs
@@
         files.sort();
         files.dedup();
+        files.retain(|path| {
+            let lower = path.to_lowercase();
+            if lower.contains(".zip/") || lower.ends_with(".zip") {
+                warn!(
+                    "Skipping ZIP archive entry '{}'; extract the archive before running blissify.",
+                    path
+                );
+                return false;
+            }
+            true
+        });

         Ok(files)
     }
