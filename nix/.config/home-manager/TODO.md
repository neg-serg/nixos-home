# TODO

- [ ] Teach `nix flake check` (or a dedicated `hm-extras` job) to build `pkgs.neg.cantata` so the
      patched package stays tested automatically.
- [ ] Document the Firefox multi-profile stack (profiles, desktop entries, and addon requirements) in the READMEs once the layout stabilizes.
