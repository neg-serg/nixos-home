_: {
  programs = {
    # Optional prompt theming; disable to reduce activation steps
    oh-my-posh = {
      enable = false;
      useTheme = "atomic";
    };
    nix-your-shell.enable = true;
  };
}
